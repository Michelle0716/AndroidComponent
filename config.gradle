ext {
    isBuildApp=false;//false:作为Lib组件存在， true:作为application存在
    //注意：这里组件化是一个思想，在为一个app的时候，可以调用，完全打包进去，
    // ture的时候是各自的app，会出现异常，因为是各自存在的独立个体，低耦合。sample没有登陆和商店模块。

    android = [
            compileSdkVersion: 23,
            buildToolsVersion: "23.0.2",
            minSdkVersion    : 16,
            targetSdkVersion : 22,
            versionCode :1,
            versionName : '1.0.0',

    ]

    dependencies = [
            appcompatV7 : 'com.android.support:appcompat-v7:23.0.1',
            design: 'com.android.support:design:23.0.1',
            umeng:  'com.umeng.analytics:analytics:latest.integration',
            glide:  'com.github.bumptech.glide:glide:3.7.0',
            reactnative:   'com.facebook.react:react-native:+',
            lmbanners:  'com.allure0:LMBanners:1.0.8',

    ]
}